<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Chat App</title>
    <script>
        const roomName = "<%= roomName %>"
    </script>
    <script defer src="https://cdnjs.cloudflare.com/ajax/libs/socket.io/2.3.0/socket.io.js">
    </script>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css"
        integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <script defer src="./script.js"></script>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css"
        integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <style>
        img {
            border-radius: 50%;
            height: 50px;
            width: 50px;
        }

        #message-container div {
            background-color: white;
            font-weight: bold;
            border: 1px solid black;
            padding: 9px;
            border-radius: 10px;
        }

        #message-container div:nth-child(2n) {
            padding: 9px;
            font-weight: bold;
            border-radius: 10px;
            background-color: rgb(0, 115, 255);
            color: white;
        }

        .twit-container {
            height: 200px;
            display: inline-block;
            overflow: scroll;
        }


        input {
            width: 90% !important;
        }

        #message-container {
            height: 460px;
            overflow: auto;
        }

        .card-body {
            font-size: 14px;
        }
    </style>
</head>

<body>
    <nav style="text-align: center;"><a href="/">[View all Rooms]</a></nav>
    <div class="container">
        <h4>Live Tweets</h4>
        <div class="twit-container">
            <% for(var i=0; i < data.length; i++) { %>
            <div class="card">
                <div class="card-body">
                    <img src="<%= data[i].user.profile_image_url %>">
                    <p style="color: red;"><%= data[i].user.name %></p>
                    <p> <%= data[i].text %> </p><span><a target="_blank"
                            href="https://twitter.com/<%=data[i].user.screen_name%>">now</a></span>
                </div>
            </div>
            <% } %>
        </div>
        <div>
            <div>
                <h3>Live Feed</h3>
                <div id="message-container"></div>
            </div>
            <div>
                <br>
                <form class="form-inline" id="send-container">
                    <input class="form-control" type="text" id="message-input">
                    <button class="btn btn-primary" type="submit" id="send-button">Send</button>
                </form>
            </div>
        </div>
    </div>
    <script>
        var objDiv = document.getElementById("message-container");
        objDiv.scrollTop = 1000;
    </script>
</body>

</html>